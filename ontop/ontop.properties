# Ontop VKG endpoint — connection basics. Credentials come from the
# ONTOP_DB_USER / ONTOP_DB_PASSWORD environment variables set in the compose
# files, never from this file.
jdbc.url = jdbc:postgresql://postgres:5432/cardmaker
jdbc.driver = org.postgresql.Driver
